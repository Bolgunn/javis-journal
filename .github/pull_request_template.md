## What & why

<!-- One or two lines. `closes #N` if it came from an issue. -->

## Checked on the phone

<!-- The Vercel preview is the device rig: what did you actually pinch/tap/cut? "n/a" if not UI. -->

## Migrations

<!-- Delete this section if the PR has no files under supabase/migrations/. -->

- [ ] Additive-first: this migration adds; anything it replaces is dropped in a *later* PR
- [ ] Contains no personal data (the allowlist is managed in the Supabase SQL editor)
- [ ] I ran `supabase db push` against production **before** merging
